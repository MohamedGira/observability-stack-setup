route:
  receiver: "email"

receivers:
  - name: "email"
    email_configs:
      - to: "you@example.com"
        from: "alert@example.com"
        smarthost: "smtp.example.com:587"
        auth_username: "user"
        auth_password: "password"